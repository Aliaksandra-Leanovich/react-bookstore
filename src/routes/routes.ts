export enum routes {
  HOME = "/",
  CART = "/cart",
  NEWBOOKS = "/newbooks",
  BOOKDETAILS = "/books/:id",
  FAVORITES = "/favorites",
  ACCOUNT = "/account",
  SEARCH = "/search/:title/:page",
  SIGNIN = "/sign-in",
  SIGNUP = "/sign-up",
  NOT_FOUND = "*",
}
